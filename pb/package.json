{
	"name": "pb-hooks-starter",
	"version": "0.0.1",
	"license": "MIT",
	"scripts": {
		"build": "tsup ./src/entries/*.ts -d ../deploy/pb_hooks",
		"watch": "chokidar ./src/** -c \"pnpm build\" --initial",
		"dev": "pocketbase serve --hooksDir=../deploy/pb_hooks --publicDir=../deploy/pb_public"
	},
	"dependencies": {
		"chokidar-cli": "^3.0.0",
		"tsup": "^8.3.5",
		"typescript": "^5.6.3"
	},
	"devDependencies": {
		"pocketbase-jsvm": "^0.22.20002"
	}
}
